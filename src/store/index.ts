
import photoAction from './photos/photo.action';
import photoReducer from './photos/photo.reducer';

const Actions = {
	photoAction,
};

const Reducers = {
	photoReducer,
};

export {
	Actions,
	Reducers,
};
